<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      The UI is fully Compose (androidx.tv.material3); this XML theme only provides a dark,
      action-bar-free window so there's no flash before Compose draws. Based on the platform
      Material theme to avoid pulling in AppCompat.
    -->
    <style name="Theme.DxAmbient" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>

    <!--
      System splash (Android 12+ via androidx.core:core-splashscreen): the neon DX logo on the
      brand background, then hands off to the normal app theme.
    -->
    <style name="Theme.DxAmbient.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.DxAmbient</item>
    </style>
</resources>
