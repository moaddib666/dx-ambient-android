# Built application files / build output (any module, any depth)
build/
*.apk
*.aar
*.ap_
*.aab
*.dex
*.class

# Gradle
.gradle/
captures/
.externalNativeBuild/
.cxx/

# Kotlin
.kotlin/

# Local configuration (SDK path, secrets) — never commit
local.properties

# IDE
*.iml
.idea/
.DS_Store

# Keystores & signing secrets — NEVER commit (allow a shared debug keystore)
*.keystore
*.jks
keystore.properties
!debug.keystore

# Google Play service-account key (Gradle Play Publisher) — NEVER commit
play-service-account.json

# Logs
*.log

# Working scratch / source assets staged for import
tmp/
